import { PII_SERVICES } from "@/constants/pii/work/services";
import { PII_PORTFOLIOS } from "@/constants/pii/work/portfolios";

export const PII_WORKS = {
    services: PII_SERVICES,
    portfolios: PII_PORTFOLIOS,
}