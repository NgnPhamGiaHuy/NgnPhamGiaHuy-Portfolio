/**
 * Interface for skill item properties
 * @interface SkillInterface
 * @property {string} name - The name of the skill or technology
 * @property {string} url - The URL to the skill's icon or resource
 */
export interface SkillInterface {
    name: string;
    url: string;
}
