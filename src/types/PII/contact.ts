/**
 * Interface for contact information properties
 * @interface ContactProps
 * @property {string} email - The email address for contact
 * @property {string} phone - The phone number for contact
 * @property {string} location - The physical location or address
 */
export interface ContactProps {
    email: string;
    phone: string;
    location: string;
}
